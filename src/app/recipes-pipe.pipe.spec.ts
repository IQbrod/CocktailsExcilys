import { RecipesPipePipe } from './recipes-pipe.pipe';

describe('RecipesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
